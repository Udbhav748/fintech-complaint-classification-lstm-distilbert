# Core tabular data & scientific computation
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Deep learning & representations
torch>=2.0.0
tensorflow>=2.15.0
# D0 (Task 13) uses the HuggingFace TensorFlow path (project_plan.md Locked
# Summary). transformers>=5.0 dropped TF model classes entirely (TFAutoModel*
# no longer exist) - pin below that. tf_keras is also required alongside
# TF's native Keras 3 for the TF model classes to import at all.
transformers>=4.35.0,<5.0.0
tf_keras>=2.15.0
accelerate>=0.25.0
tokenizers>=0.15.0

# Visualization & utilities
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# Demo UI
streamlit>=1.30.0
